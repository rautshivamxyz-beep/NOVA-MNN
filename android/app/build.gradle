plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    androidResources {
        noCompress += ["gguf"]
    }
    namespace 'org.nova'
    compileSdk 35

    packagingOptions {
        jniLibs {
            useLegacyPackaging true
        }
    }

    sourceSets {
        main {
            jniLibs.srcDirs += ['src/main/jniLibs']
        }
    }

    defaultConfig {
        applicationId 'org.nova.mnn'
        minSdk 30
        targetSdk 35
        versionCode 26
        versionName '5.4.1-M1'
    }

    signingConfigs {
        release {
            storeFile rootProject.file('nova-release.keystore')
            storePassword 'NOVA2026release'
            keyAlias 'nova'
            keyPassword 'NOVA2026release'
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.release
        }
    }

    lint {
        checkReleaseBuilds false
        abortOnError false
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    // MNN inference engine (org.nova.mnn)
    implementation files('libs/mnn-release.aar')

    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.9.0'
    implementation 'androidx.recyclerview:recyclerview:1.3.2'
    implementation 'io.noties.markwon:core:4.6.2'
    implementation 'io.noties.markwon:syntax-highlight:4.6.2'
    implementation 'io.noties:prism4j:2.0.0'
    implementation 'com.tom-roush:pdfbox-android:2.0.27.0'

    // on-device OCR for scanned PDFs (bundled model - fully offline)
    implementation 'com.google.mlkit:text-recognition:16.0.1'
}

configurations.all {
    exclude group: 'org.jetbrains', module: 'annotations-java5'
}
